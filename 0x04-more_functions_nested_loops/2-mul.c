#include "main.h"
/**
 * mul - multiplies two ints
 * @a: the first params
 *@b: the second params
 * Return: 0 always for success
 */

int mul(int a, int b)
{
	int c;
	/*Some computations*/
	c = a * b;
	return (c);
}
