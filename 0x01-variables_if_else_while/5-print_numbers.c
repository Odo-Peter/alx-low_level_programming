#include <stdio.h>
/**
 * main - prints out numbers less than 10
 * Return: 0 for completion
*/
int main(void)
{
	int i;

	/*The for loop for the numbers being printed*/
	for (i = 0; i < 10; i++)
	{
		printf("%d\n", i);
	}
	return(0);
}
