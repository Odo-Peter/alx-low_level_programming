#include <stdio.h>
/**
 * main - prints the ints values 1 - 10
 * Return: 0 for completion
 */

int main(void)
{
	int i;

	/*Performing the for loop*/
	for (i = 65; i < 75; i++)
	{
		putchar(i);
	}
	putcar('\n');
	return (0);
}
